[project]
name = "intel-feed-finder"
version = "0.1.0"
description = "RSS/Atom/JSON feed discovery utility for threat intelligence sources"
requires-python = ">=3.10"
dependencies = [
    "feedparser>=6.0.0",
    "requests>=2.28.0",
    "beautifulsoup4>=4.12.0",
]

[tool.pdm]
distribution = false
